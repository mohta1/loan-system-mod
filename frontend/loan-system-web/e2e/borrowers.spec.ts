import{expect,test}from'@playwright/test';
test('TASK-03 borrower CRUD lifecycle remains persisted and readable',async({page})=>{const username=process.env.E2E_ADMIN_USERNAME,password=process.env.E2E_ADMIN_PASSWORD;test.skip(!username||!password,'Disposable credentials must be injected');const unique=`${Date.now()}-${Math.random().toString(16).slice(2)}`,civil=`e2e-civil-${unique}`,employee=`e2e-employee-${unique}`;await page.goto('/');await page.getByLabel('Username').fill(username!);await page.getByLabel('Password').fill(password!);await page.getByRole('button',{name:'Login'}).click();await page.getByRole('button',{name:'Borrowers'}).click();await page.getByRole('button',{name:/Create Borrower/}).click();await page.getByLabel(/Civil Number/).fill(civil);await page.getByLabel(/Employee Number/).fill(employee);await page.getByLabel(/Full Name/).fill('TASK-03 E2E Borrower');await page.getByLabel(/Phone Number/).fill('90000000');await page.getByLabel(/Nationality/).fill('Omani');await page.getByLabel(/Organization/).fill('E2E Organization');await page.getByLabel(/Rank \/ Grade/).fill('G7');await page.getByRole('button',{name:'Create'}).click();await page.getByLabel('Civil Number').fill(civil);await page.getByRole('button',{name:'Search'}).click();await expect(page.getByText('TASK-03 E2E Borrower')).toBeVisible();await page.getByText('TASK-03 E2E Borrower').click();await expect(page.getByText('E2E Organization')).toBeVisible();await page.getByRole('button',{name:'Edit'}).click();await expect(page.getByLabel(/Full Name/)).toHaveValue('TASK-03 E2E Borrower');await page.getByLabel(/Organization/).fill('Persisted E2E Organization');await page.getByRole('button',{name:'Save'}).click();await page.getByLabel('Civil Number').fill(civil);await page.getByRole('button',{name:'Search'}).click();await page.getByText('TASK-03 E2E Borrower').click();await expect(page.getByText('Persisted E2E Organization')).toBeVisible();page.once('dialog',dialog=>dialog.accept());await page.getByRole('button',{name:'Deactivate'}).click();await expect(page.getByText('Inactive')).toBeVisible();await page.getByRole('button',{name:'Back'}).click();await page.getByLabel('Status').selectOption('Inactive');await page.getByRole('button',{name:'Search'}).click();await page.getByText('TASK-03 E2E Borrower').click();await expect(page.getByText('Inactive')).toBeVisible();page.once('dialog',dialog=>dialog.accept());await page.getByRole('button',{name:'Activate'}).click();await expect(page.getByText('Active')).toBeVisible();await page.getByRole('button',{name:'Logout'}).click();await expect(page.getByRole('heading',{name:'Sign in'})).toBeVisible()});
