import{beforeEach,expect,it,vi}from'vitest';import{loanProductsApi}from'./loanProducts';
const ok=(body:unknown)=>Promise.resolve(new Response(JSON.stringify(body),{status:200,headers:{'Content-Type':'application/json'}}));
beforeEach(()=>vi.stubGlobal('fetch',vi.fn(()=>ok({}))));
it('uses every resource endpoint with server concurrency tokens',async()=>{const v={versionId:'v',versionNumber:1,status:'Draft',eTag:'tag'}as never;const p={loanProductId:'p',status:'Active',eTag:'ptag'}as never;await loanProductsApi.list();await loanProductsApi.get('p');await loanProductsApi.create('Product');await loanProductsApi.draft('p',{}as never);await loanProductsApi.edit('p',v,{}as never);await loanProductsApi.publish('p',v);await loanProductsApi.status(p,false);await loanProductsApi.available();expect(fetch).toHaveBeenCalledTimes(8);expect(new Headers(vi.mocked(fetch).mock.calls[4][1]?.headers).get('If-Match')).toBe('"tag"');});
it('invalidates the session and exposes stable API errors',async()=>{const handler=vi.fn();addEventListener('identity:unauthorized',handler);vi.mocked(fetch).mockResolvedValue(new Response(JSON.stringify({errorCode:'loanProducts.validation'}),{status:401}));await expect(loanProductsApi.list()).rejects.toMatchObject({status:401,errorCode:'loanProducts.validation'});expect(handler).toHaveBeenCalled();removeEventListener('identity:unauthorized',handler);});
