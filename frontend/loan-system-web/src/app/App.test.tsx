import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{beforeEach,describe,expect,it,vi}from'vitest';import{App}from'./App';import{hasPermission}from'./permissions';import{applyLanguage}from'../i18n';
const renderApp=()=>render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false},mutations:{retry:false}}})}><App/></QueryClientProvider>);const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{'Content-Type':'application/json'}});
describe('identity UI',()=>{beforeEach(()=>{applyLanguage('en');history.replaceState({},'', '/');vi.stubGlobal('fetch',vi.fn())});it('shows login after unauthenticated me and generic failure',async()=>{vi.mocked(fetch).mockResolvedValueOnce(json({},401)).mockResolvedValueOnce(json({errorCode:'identity.invalidCredentials'},401));renderApp();await userEvent.type(await screen.findByLabelText('Username'),'nobody');await userEvent.type(screen.getByLabelText('Password'),'wrong');await userEvent.click(screen.getByRole('button',{name:'Login'}));expect(await screen.findByRole('alert')).toHaveTextContent('Invalid username or password')});it('logs in and renders authenticated shell',async()=>{vi.mocked(fetch).mockResolvedValueOnce(json({},401)).mockResolvedValueOnce(new Response(null,{status:204})).mockResolvedValueOnce(json({userId:'1',username:'admin',displayName:'Administrator',permissions:['identity.users.manage']}));renderApp();await userEvent.type(await screen.findByLabelText('Username'),'admin');await userEvent.type(screen.getByLabelText('Password'),'password12345');await userEvent.click(screen.getByRole('button',{name:'Login'}));expect(await screen.findByText('Administrator')).toBeInTheDocument();expect(screen.getByRole('button',{name:'User Administration'})).toBeInTheDocument()});it('hides administration without permission and logs out',async()=>{vi.mocked(fetch).mockResolvedValueOnce(json({userId:'1',username:'user',displayName:'User',permissions:[]})).mockResolvedValueOnce(new Response(null,{status:204}));renderApp();expect(await screen.findByText('User')).toBeInTheDocument();expect(screen.queryByText('User Administration')).not.toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Logout'}));expect(await screen.findByText('Sign in')).toBeInTheDocument()});it('lists users and performs activate and role assignment',async()=>{const user={userId:'u',username:'test',displayName:'Test',isActive:true,roleIds:[],eTag:'v'};vi.mocked(fetch).mockResolvedValueOnce(json({userId:'1',username:'admin',displayName:'Admin',permissions:['identity.users.manage']})).mockResolvedValueOnce(json([user])).mockResolvedValueOnce(json([{roleId:'r',name:'Viewer'}])).mockResolvedValue(json(user));renderApp();await userEvent.click(await screen.findByRole('button',{name:'User Administration'}));expect((await screen.findAllByText('test')).length).toBeGreaterThan(0);await userEvent.click(screen.getByRole('button',{name:'Edit'}));await userEvent.click(screen.getByLabelText('Viewer'));await userEvent.click(screen.getByRole('button',{name:'Save'}));expect(fetch).toHaveBeenCalledWith('/api/v1/users/u/roles',expect.objectContaining({method:'PUT',headers:{'If-Match':'"v"'}}))});it('switches Arabic RTL',async()=>{vi.mocked(fetch).mockResolvedValue(json({},401));renderApp();await userEvent.click(await screen.findByRole('button',{name:'العربية'}));expect(document.documentElement).toHaveAttribute('dir','rtl');expect(screen.getByText('تسجيل الدخول')).toBeInTheDocument()})});
it('evaluates permissions exactly',()=>{expect(hasPermission(['borrowers.read'],'borrowers.read')).toBe(true);expect(hasPermission(['borrowers.read'],'identity.users.manage')).toBe(false)});
describe('baseline utilities',()=>{it('calls system information API',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(json({version:'1'})));const{getSystemInfo}=await import('../api/system');expect((await getSystemInfo()).version).toBe('1')});it('renders the error boundary fallback',async()=>{const{ErrorBoundary}=await import('./ErrorBoundary');const Broken=()=>{throw new Error('broken')};vi.spyOn(console,'error').mockImplementation(()=>undefined);render(<ErrorBoundary><Broken/></ErrorBoundary>);expect(screen.getByRole('alert')).toHaveTextContent('Application unavailable')})});

describe('hardened auth behavior',()=>{beforeEach(()=>{applyLanguage('en');vi.stubGlobal('fetch',vi.fn())});it('shows API error rather than login for me 500',async()=>{vi.mocked(fetch).mockResolvedValue(json({},500));renderApp();expect(await screen.findByText('The application service is unavailable.')).toBeInTheDocument();expect(screen.queryByText('Sign in')).not.toBeInTheDocument()});it('keeps shell when logout fails',async()=>{vi.mocked(fetch).mockResolvedValueOnce(json({userId:'1',username:'u',displayName:'User',roles:[],permissions:[]})).mockResolvedValueOnce(json({},500));renderApp();await userEvent.click(await screen.findByRole('button',{name:'Logout'}));expect(await screen.findByText('Logout failed. Please try again.')).toBeInTheDocument();expect(screen.getByText('User')).toBeInTheDocument()});it('clears shell when protected request returns 401',async()=>{vi.mocked(fetch).mockResolvedValueOnce(json({userId:'1',username:'admin',displayName:'Admin',roles:[],permissions:['identity.users.manage']})).mockResolvedValueOnce(json({},401)).mockResolvedValueOnce(json([],200));renderApp();await userEvent.click(await screen.findByRole('button',{name:'User Administration'}));expect(await screen.findByText('Sign in')).toBeInTheDocument()});it('preserves unrelated roles when selecting another',async()=>{const user={userId:'u',username:'test',displayName:'Test',isActive:true,roleIds:['r1'],eTag:'v'};vi.mocked(fetch).mockResolvedValueOnce(json({userId:'1',username:'admin',displayName:'Admin',roles:[],permissions:['identity.users.manage']})).mockResolvedValueOnce(json([user])).mockResolvedValueOnce(json([{roleId:'r1',name:'Reader'},{roleId:'r2',name:'Writer'}])).mockResolvedValue(json(user));renderApp();await userEvent.click(await screen.findByRole('button',{name:'User Administration'}));await userEvent.click(await screen.findByRole('button',{name:'Edit'}));expect(screen.getByLabelText('Reader')).toBeChecked();await userEvent.click(screen.getByLabelText('Writer'));expect(fetch).not.toHaveBeenCalledWith('/api/v1/users/u/roles',expect.anything());await userEvent.click(screen.getByRole('button',{name:'Save'}));expect(fetch).toHaveBeenCalledWith('/api/v1/users/u/roles',expect.objectContaining({body:JSON.stringify({roleIds:['r1','r2']})}))})});
describe('user administration dialogs',()=>{beforeEach(()=>{applyLanguage('en');vi.stubGlobal('fetch',vi.fn())});it('creates a user with an initial role and resets dialog',async()=>{const created={userId:'new',username:'new-user',displayName:'New User',isActive:true,roleIds:[],eTag:'v'};vi.mocked(fetch).mockResolvedValueOnce(json({userId:'admin',username:'admin',displayName:'Admin',roles:[],permissions:['identity.users.manage']})).mockResolvedValueOnce(json([])).mockResolvedValueOnce(json([{roleId:'r1',name:'Reader'}])).mockResolvedValueOnce(json(created,201)).mockResolvedValueOnce(json({...created,roleIds:['r1']})).mockResolvedValue(json([created]));renderApp();await userEvent.click(await screen.findByRole('button',{name:'User Administration'}));await userEvent.click(await screen.findByRole('button',{name:/Create User/}));await userEvent.type(screen.getByLabelText('Username'),'new-user');await userEvent.type(screen.getByLabelText('Display name'),'New User');await userEvent.type(screen.getByLabelText('Password'),'password-1234');await userEvent.click(screen.getByLabelText('Reader'));await userEvent.click(screen.getByRole('button',{name:'Create'}));expect(await screen.findByText('new-user')).toBeInTheDocument()});it('cancels create and edit without mutation',async()=>{const user={userId:'u',username:'test',displayName:'Test',isActive:false,roleIds:[],eTag:'v'};vi.mocked(fetch).mockResolvedValueOnce(json({userId:'admin',username:'admin',displayName:'Admin',roles:[],permissions:['identity.users.manage']})).mockResolvedValueOnce(json([user])).mockResolvedValueOnce(json([]));renderApp();await userEvent.click(await screen.findByRole('button',{name:'User Administration'}));await userEvent.click(await screen.findByRole('button',{name:/Create User/}));await userEvent.click(screen.getByRole('button',{name:'Cancel'}));await userEvent.click(screen.getByRole('button',{name:'Edit'}));await userEvent.clear(screen.getByLabelText('Display name'));await userEvent.type(screen.getByLabelText('Display name'),'Changed');await userEvent.click(screen.getByRole('button',{name:'Cancel'}));expect(fetch).not.toHaveBeenCalledWith('/api/v1/users/u',expect.anything());expect(screen.getByText('Disabled')).toBeInTheDocument()})});
