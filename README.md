# Loan System Implementation Plan — Revised

This package contains the Codex-ready implementation plan after review against the source Scope.

Before TASK-00, put these two files in the repository `docs/` folder together with the earlier design documents:

* `loan-system-source-scope.md`
* `loan-system-scope-alignment.md`

Then execute tasks one at a time starting with `docs/tasks/TASK-00-bootstrap-runtime.md`.

Every task now requires: latest-main pull, document consistency check, fresh branch, complete backend/frontend vertical slice, high-coverage automated tests, quality gates, application restart, detailed user manual UI scenarios, branch push, Pull Request, green GitHub CI, and **no Codex merge**.

